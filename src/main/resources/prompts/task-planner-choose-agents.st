Given a list of tasks and available agents, determine which agent is most capable of accomplishing the task.
If you are unable to determine which agent is most capable of accomplishing the task, return None.

The tasks are:
{tasks}

Each agent has a name, a goal, and a list of tools available from the agent. Here are the list of agents:
{agents}

The map should be keyed by the task and the value should be the agent most capable of accomplishing the task.
It's extremely important that the returned map size should equal the number of tasks.
{format}
