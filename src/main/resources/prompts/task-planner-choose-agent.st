Given a task and a list of agents, determine which of the agents is most capable of accomplishing the task.
Return just the name of the agent with no extra commentary.
If no agent is capable of accomplishing the task, just return "DefaultLLMAgent" and nothing else.

The task is:
{task}

Each agent has a name and a goal. Here are the list of agents:
{agents}

